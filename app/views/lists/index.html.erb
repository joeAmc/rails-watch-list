<h1>List</h1>
<ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, list_path(list) %></li>
  <% end %>
</ul>
<p><%= link_to 'Add List', new_list_path(@list), class: 'btn btn-success mt-4' %></p>
